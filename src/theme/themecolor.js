import { createTheme } from '@mui/material/styles';

const themeColor = createTheme({
    palette: {
      primary: {
        main: "#21415b",
      }, 
      secondary: {
        main: "#008080 ",
      }
    },
  });
  
  export default themeColor