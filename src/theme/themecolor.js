import { createTheme } from '@mui/material/styles';

const themeColor = createTheme({
    palette: {
      primary: {
        main: "#21415b",
      }, 
      secondary: {
        main: "#fff ",
      }
    },
  });
  
  export default themeColor